@model IEnumerable<Emuher.Models.AracPlaza>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>Plazalardaki Stok Durumu</h2>

<p>
    @Html.ActionLink("Yeni Oluştur", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Model.ModelAdi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Plaza.PlazaAdi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Stok)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Model.ModelAdi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Plaza.PlazaAdi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Stok)
        </td>
        <td>
            @Html.ActionLink("Düzenle", "Edit", new { id=item.AracPlazaID }) |
            @Html.ActionLink("Sil", "Delete", new { id=item.AracPlazaID })
        </td>
    </tr>
}

</table>
